#include <bits/stdc++.h>
using namespace std;
typedef long long  ll;
typedef vector<int> VI;
typedef vector<ll> VLL;
#define rep(i,n) for(int i=0; i<(int)(n); i++)

int main(){
    int n,m;
    cin >> n >> m;
    string ans;
    if(m == n)ans = "Yes";
    else ans = "No";

    cout << ans << endl;
}