
#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); ++i)
using namespace std;

int main() {
    int a, b;
    while (cin >> a >> b) {
        if (a == b) {
            cout << "Yes";
        } else {
            cout << "No";
        }
        cout << endl;
    }
}

