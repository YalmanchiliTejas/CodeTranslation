#include <bits/stdc++.h>

#define rep(i, n) for(int (i) = 0; i < (n); ++(i))

typedef long long ll;
const ll MOD = 1e9 + 7;

int main() {
  int N, M;
  std::cin >> N >> M;

  if(N == M) {
    std::cout << "Yes" << '\n';
  } else {
    std::cout << "No" << '\n';
  }
}
