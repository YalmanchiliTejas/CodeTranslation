#include <bits/stdc++.h>
#define rep(i, n) for(int i=0; i<(n); i++)
#define itrep(type, it, t) for(type::iterator it=t.begin(); it!=t.end(); it++)
using namespace std;


int main(void) {

    int N, M;
    cin >> N >> M;
    cout << (N == M ? "Yes" : "No");

    return 0;
}