#include <cmath>
#include <cstdlib>
#include <iostream>
#include <string>

using namespace std;

int main() {
  int n, m;
  scanf("%d %d", &n, &m);


  cout << ((n == m) ? "Yes" : "No") << endl;
  
  return 0;
}
