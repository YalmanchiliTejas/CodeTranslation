#include <cstdio>
#include <algorithm>
#include <vector>
#include <string.h>
#include <queue>
#include <math.h>
#include <string>
#include <iostream>
using namespace std;
typedef long long ll;

int main()
{
	int N, M;
	scanf("%d %d", &N, &M);
	N == M ? puts("Yes") : puts("No");
	return 0;
}