#include <algorithm>
#include <iostream>
#include <vector>
#include <map>
#define rep(i, n) for (int i = 0; i < (n); i++)
typedef long long ll;

using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  string ans = (n == m) ? "Yes" : "No";
  cout << ans << endl;
}
