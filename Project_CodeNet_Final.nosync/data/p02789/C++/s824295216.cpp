#include <bits/stdc++.h>

using namespace std;

#define ll long long

int main() {
    cin.tie();
    ios_base::sync_with_stdio(false);
    
    int a, b; cin >> a >> b; cout << (a == b ? "Yes" : "No");
    return 0;
}