#include <iostream>
#include <algorithm> 
#include <cstring>
#include <cmath>
#include <queue>
using namespace std;

int main() {
	int n, m;
	cin >> n >> m;
	cout << (n == m ? "Yes" : "No");
	return 0;
}
