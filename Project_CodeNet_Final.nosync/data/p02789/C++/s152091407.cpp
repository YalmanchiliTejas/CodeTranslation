#include <iostream>
#include <iomanip>
#include <string>
#include <cmath>
#include <algorithm>
#include <vector>
#include <map>
#include <set>
#include <queue>
#include <tuple>

using namespace std;
typedef long long ll;

#define MAX_N 10000
int a[MAX_N];

int main() {

  // -----------------------------
  cin.tie(nullptr);
  ios::sync_with_stdio(false);

  // input
  int N, M;
  cin >> N >> M;

  // -----------------------------

  if ( N == M ) cout << "Yes" << endl;
  else cout << "No" << endl;
  
  return 0;
  
}
