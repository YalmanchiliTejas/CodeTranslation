#include <bits/stdc++.h>
#define ll long long
using namespace std;
#define all(v) v.begin(), v.end()
#define rep(i, n) for (ll i = 0; i < (int)(n); i++)

int main(void){
    int n,m;
    cin >> n >> m;
    cout << (n==m ? "Yes" : "No") << endl;
    return 0;
}