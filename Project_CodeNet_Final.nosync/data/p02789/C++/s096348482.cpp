#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main(void)
{
  ios_base::sync_with_stdio(false);
  cin.tie(0); cout.tie(0);

  int N, M; cin >> N >> M;

  if (N <= M)
    cout << "Yes" << endl;
  else
    cout << "No" << endl;

  return 0;
}
