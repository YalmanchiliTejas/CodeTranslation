#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int debug = 0;

int main()
{
    int n, m;
    cin >> n >> m;
    if(n-m == 0)
        printf("Yes\n");
    else
        printf("No\n");
}