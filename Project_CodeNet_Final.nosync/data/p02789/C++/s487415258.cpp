#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
 
int main() {
  int N, M;
  cin >> N;
  cin >> M;
  if (N == M) {
    printf("Yes");
  } else {
    printf("No");
  }
  return 0;
}