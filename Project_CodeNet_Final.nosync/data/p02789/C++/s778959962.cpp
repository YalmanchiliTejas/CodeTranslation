#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
// #include <iomanip>
// std::cout << std::fixed << std::setprecision(5);

int main()
{
    int m, n;
    std::cin >> n >> m;
    std::cout << ((n==m) ? "Yes" : "No") << std::endl;
    return 0;
}