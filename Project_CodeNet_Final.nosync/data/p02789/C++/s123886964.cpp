#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

const string YES = "Yes";
const string NO = "No";
int main(){
  int N, M; cin >> N >> M;
  if (N == M) cout << YES << endl;
  else cout << NO << endl;
}
