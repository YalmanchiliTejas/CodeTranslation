#include<bits/stdc++.h> 
using namespace std; 
#define ll long long int

  //mp.reserve(1024);
  //mp.max_load_factor(0.25);
int main() 
{ 
	ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	ll n,m;
	cin>>n>>m;
	if(m==n)
		cout<<"Yes"<<endl;
	else
		cout<<"No"<<endl;
    return 0; 
} 
