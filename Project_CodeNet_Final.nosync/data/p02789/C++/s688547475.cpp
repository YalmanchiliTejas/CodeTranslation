#include <iostream>
#include <math.h>
#include <stdio.h>
#include <algorithm>
#include <string.h>
#include <bits/stdc++.h>

using namespace std;

long long n, m;

int main () {
    cin >> n >> m;
    if ( m < n ) {
        cout <<"No";
    }
    else {
        cout <<"Yes";
    }
}
