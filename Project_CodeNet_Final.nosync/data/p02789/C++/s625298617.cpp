#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    if (m == n) {
        printf("Yes\n");
        return 0;
    } else {
        printf("No\n");
        return 0;
    }
    return 0;
}