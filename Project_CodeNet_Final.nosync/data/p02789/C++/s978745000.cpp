#include<bits/stdc++.h>
using namespace std;
#define int long long int
#define until(x) while(!(x))
#define unless(x) if(!(x))
#define all(x) (x).begin(),(x).end()

int32_t main(){
	int n,m;
	cin>>n>>m;
	if(n==m){
		cout<<"Yes";
	}
	else {
		cout<<"No";
	}
}
