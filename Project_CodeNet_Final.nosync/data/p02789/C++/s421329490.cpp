#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define REP(i, a, b) for(int i = a, i##_len = (b); i < i##_len; ++i)

int main()
{
    int N, M;
    cin >> N >> M;
    if(M < N) cout << "No" << endl;
    else cout << "Yes" << endl;
    return 0;
}