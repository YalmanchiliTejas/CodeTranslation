#include <bits/stdc++.h>
#define for(a,n) for(int i = int(a); i<int(n); i++)
#define ll long long

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m;
    cin>>n>>m;
    if(n==m)
        cout<<"Yes";
    else
        cout<<"No";












    return 0;
}
