#include <bits/stdc++.h>
#define rep(i, N) for (int i=0; i<(int)N; i++)
#define rep1(i, N) for (int i=1; i<(int)N; i++)

using namespace std;

int n, m;

int main() {
  cin >> n >> m;
  if (n == m) cout << "Yes";
  else cout << "No";
}
