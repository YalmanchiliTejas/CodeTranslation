#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<ll,ll> pll;
typedef map<ll,ll> mll;
typedef set<ll> sll;

#define se second
#define fi first
#define pb push_back

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

  ll n,m;
  cin>>n>>m;
  if(m==n)
    cout<<"Yes";
  else
    cout<<"No";

}
