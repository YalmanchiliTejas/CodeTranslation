#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <utility>
using namespace std;
typedef unsigned long long ll;
#define rep(i, n) for (int i=0;i < (int)(n);i++)

int main(){
    int n,a;
    string s;
    cin >> n >> a;
    if (n==a) cout << "Yes" << endl;
    else cout << "No"<< endl;
    
    return 0;
}
