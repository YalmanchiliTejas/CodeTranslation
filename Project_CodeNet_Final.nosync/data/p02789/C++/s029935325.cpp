#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
typedef long long ll;
int main() {
    // ifstream in("data1.txt");
    // cin.rdbuf(in.rdbuf());
    int N,M;
    cin >> N>>M;
    if(N==M){
        cout << "Yes" << endl;
    } else {
        cout << "No" << endl;
    }
}