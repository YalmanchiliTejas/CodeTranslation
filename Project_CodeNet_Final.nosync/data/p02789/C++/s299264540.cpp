#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
typedef long long ll;
const int INF = 1e9;

int main() {
  ll n, m, ans = 0;
  cin >> n >> m;

  cout << (n == m ? "Yes" : "No") << endl;
  return 0;
}
