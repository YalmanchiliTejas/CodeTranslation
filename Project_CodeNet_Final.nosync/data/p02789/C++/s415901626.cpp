#include <bits/stdc++.h>
using namespace std;                                                                                   
#define FOR(v1, v2) for (v1 = 0; v1 < v2; ++v1)
int main () {
  int n, m;
  cin >> n >> m;
  cout << ((n == m)? "Yes" : "No") << endl;
  return 0;
}