#include <bits/stdc++.h>

 
using namespace std;
 
#define ll long long
#define ul unsigned long long
#define snd second
#define fst first


int main(){
    std::ios::sync_with_stdio(false);
    ll a, b; cin >> a >> b;
    if(a == b) cout << "Yes";
    else cout << "No";
    return 0;
}
