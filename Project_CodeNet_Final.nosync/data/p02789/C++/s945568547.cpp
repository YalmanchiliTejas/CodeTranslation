#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
int main() {
  int n, m;
  scanf("%d %d", &n, &m);
  if (n == m) printf("Yes\n");
  else printf("No\n");
  return 0;
}
