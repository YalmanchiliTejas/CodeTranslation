#include <bits/stdc++.h>
#define fastio() ios_base::sync_with_stdio(false);
using namespace std;
typedef long long ll;
typedef long double ld;


int main(){
	int n, m;
	cin >> n >> m;
	if (n == m) puts("Yes");
	else puts("No");
	return 0;
}
