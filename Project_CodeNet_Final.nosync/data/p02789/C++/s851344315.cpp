#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); ++i)
typedef long long int64;

int main() {
    int a, b;
    cin >> a >> b;
    cout << (a == b ? "Yes" : "No") << '\n';
    return 0;
}
