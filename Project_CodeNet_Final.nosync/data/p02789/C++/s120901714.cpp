#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <iomanip>
#include <functional>
#include "math.h"
#include <set>
using namespace std;
#define FOR(i,a,b) for (int i=(a);i<(b);i++)
#define RFOR(i,a,b) for (int i=(b)-1;i>=(a);i--)
#define REP(i,n) for (int i=0;i<(n);i++)
#define RREP(i,n) for (int i=(n)-1;i>=0;i--)
#define VI vector<int>
#define debug(x) cout<<#x<<": "<<x<<endl
#define DEBUG(x) cout<<#x<<": "<<x<<endl
#define MB 1000000007
#define NB 1000000000

int N,M;
vector<double> A;
int a[70];
int main()
{

	cin >> N >> M;
	if(N > M){
		cout << "No";
	} else {
		cout << "Yes";
	}

//	cout << fixed << setprecision(0) <<  N > M ? "No" : "Yes";


}


