#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,srt,end) for (long long i=(srt);i<(long long)(end);i++)
#define VL vector<ll> 
ll N,M;
int main(){
    cin>>N>>M;
    if(N==M)cout<<"Yes";
    else cout<<"No";
    return 0;
}