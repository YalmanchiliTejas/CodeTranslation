#include <iostream>
using namespace std;

int main(void) {
    int n, m;

    cin.tie(0);
    ios::sync_with_stdio(false);
    cin >> n >> m;
    cout << (n == m? "Yes" : "No") << "\n";
    return 0;
}