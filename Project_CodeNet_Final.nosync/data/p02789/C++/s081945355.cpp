#include <iostream>
#include <stdio.h>
#include <string>
#include <algorithm>
#include <cmath>
#include <vector>
#include <functional>
#include <cstdlib>
#include <map>
using namespace std;
#define REP(i,n); for(int i = 0;i < (int)(n);i++)

int main() {
	int n,m;
	
	cin >> n >> m;

	if(n==m) cout<<"Yes";
	else cout <<"No";
	return(0);
}
