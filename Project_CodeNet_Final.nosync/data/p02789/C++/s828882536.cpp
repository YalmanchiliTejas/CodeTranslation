#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (n); ++i)

typedef long long ll;

using namespace std;

int main() {
  int n, m;
  cin >> n >> m;

  string result = n == m ? "Yes" : "No";

  cout << result << endl;

  return 0;
}
