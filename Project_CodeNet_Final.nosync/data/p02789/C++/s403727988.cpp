#include <bits/stdc++.h>
using namespace std;
#include<cstdlib>

#define rep(i,n) for(int (i)=0;(i)<(n);(i)++)

int main(){
  int N, M;
  cin >> N >> M;
  if(N==M) cout << "Yes";
  else cout << "No";
}
