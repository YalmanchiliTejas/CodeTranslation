#include <bits/stdc++.h>
using namespace std;
#define FIN ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

int main() {FIN
        int n, m;
            cin >> n >> m;
            cout << (n == m ? "Yes" : "No") << "\n";
  	return 0;
        }