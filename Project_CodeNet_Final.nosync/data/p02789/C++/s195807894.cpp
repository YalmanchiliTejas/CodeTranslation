#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main()
{
    cin.tie(0);
    ios::sync_with_stdio(0);
    cout << setprecision(10);

    int n, m;
    cin >> n >> m;
    cout << (n == m ? "Yes" : "No") << endl;
}
