#include <iostream>
#include <vector>
#include <stdio.h>
using namespace std;

#define sz(s) (int)(s.size())
#define eb emplace_back

const int N = 2e5 + 5;
const int sZ = (1 << 19);

#define ll long long

void solve() {
	int n, m;
	cin >> n >> m;
	cout << (n == m ? "Yes" : "No");
}

int main () {
	cerr << "Ready\n";
	solve();
	return 0;
}
