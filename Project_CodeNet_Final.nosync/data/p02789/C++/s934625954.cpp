#include <iostream>
#include <string>
#include <vector>
#include <map>

using namespace std;
using ll = long long;

int main(){
  ll a, b;
  cin >> a >> b;
  string ans = a == b ? "Yes" : "No";
  cout << ans << endl;

  return 0;
}
