#include<iostream>
#include<vector>
#include<string>
#include<algorithm>

#define rep(i,n) for(int i=0;i<(n);i++)
typedef long long LL;
using namespace std;

int main() {
	int n, m; cin >> n >> m;

	puts(n == m ? "Yes" : "No");
	
	return 0;
}