#include<bits/stdc++.h>
#define ll long long
#define increase(a) sort(a.begin(), a.end())
#define decrease(a) sort(a.rbegin(), a.rend())
#define loop(n) for(ll i = 0; i<n; i++)
using namespace std;

int main(){
    int n, m;
    cin>>n>>m;
    if(n==m) cout<<"Yes\n";
    else cout<<"No\n";
    return 0;
}