#include <bits/stdc++.h>
using namespace std;

#define IO                       \
    std::ios_base::sync_with_stdio(false); \
    cin.tie(0);                  \
    cout.tie(0);
#define LL long long
#define DBG(vari) cerr<<#vari<<" = "<<(vari)<<endl;

const int INF = 0x3f3f3f3f;

int main(void) {
    IO;

    int n, m;

    cin >> n >> m;
    if (n == m) {
        cout << "Yes" << endl;
    } else {
        cout << "No" << endl;
    }
        

    return 0;
}
