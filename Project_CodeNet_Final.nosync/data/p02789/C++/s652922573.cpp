#include<bits/stdc++.h>
#define itn int
#define lli long long int
#define REP(a,n) for(int i=a;i < n;i++)
#define REP2(a,n) for(int j = a;j<n;j++)
using namespace std;
int main(void) {
	int N, M;
	scanf("%d %d", &N, &M);
	if (N == M) {
		printf("Yes\n");
	}
	else {
		printf("No\n");
	}
	return 0;
}