#include <bits/stdc++.h>

#ifdef _DEBUG
#define debug(x) cout<<#x<<": "<<x<<endl
#else
#define debug(x)
#endif

using namespace std;

#define mp make_pair

int main() {
    int n, m;
    cin >> n >> m;
    cout << ((n == m) ? "Yes" : "No") << endl;
}