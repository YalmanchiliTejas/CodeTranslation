#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(NULL), cout.tie(NULL);

    int N, M;
    cin >> N >> M;

    if(N == M)
        cout << "Yes\n";
    else
        cout << "No\n";

    return 0;
}
