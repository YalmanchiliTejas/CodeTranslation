#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> ii;

int n, m;
int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  cin >> n >> m;
  if (m == n)
    cout << "Yes" << endl;
  else
    cout << "No" << endl;
  
  return 0;
}
