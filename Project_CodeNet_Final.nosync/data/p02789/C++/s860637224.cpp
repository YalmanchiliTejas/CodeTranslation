#include <bits/stdc++.h>
#define rep(i,n) for (int i = 1; i < (n+1); i++)
using namespace std;
typedef long long ll;

int main(){
    int n, m;
    cin >> n >> m;
    
    if(n == m){
        cout << "Yes" << endl;
    }

    if(n != m){
        cout << "No" << endl;
    }

    return 0;
}