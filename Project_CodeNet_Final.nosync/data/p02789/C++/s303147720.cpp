#include <bits/stdc++.h>
 
using namespace std;
using ll=long long;
 
#define faster ios_base::sync_with_stdio(false),cin.tie(NULL),cout.tie(NULL)
#define cll ll t;cin>>t;while(t--)
#define rep(i,a,b) for(ll i=a;i<b;i++)
#define pii pair<int,int>
#define vint vector<int>
 
ll mod=1e9+7;
 
int main(){
    faster;
 
    int m,n;
    cin>>m>>n;
 
    if(n==m) cout<<"Yes"<<endl;
    else cout<<"No"<<endl;
}