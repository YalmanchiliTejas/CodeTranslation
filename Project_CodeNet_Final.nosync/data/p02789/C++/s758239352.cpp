#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ii = pair<int, int>;
#define sz(x) (int)(x).size()
/* --- */
int main() {
	int n, m;
	cin >> n >> m;
	if (n == m) cout << "Yes" << '\n';
	else cout << "No" << '\n';
}

