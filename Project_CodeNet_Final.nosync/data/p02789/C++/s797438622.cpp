#include <bits/stdc++.h>
using namespace std;

#define each(i, c) for (auto& i : c)

typedef long long ll;
typedef unsigned long long ull;
const ll MOD = 1e9+7;

int main() {
  ll n, m;
  cin >> n >> m;

  if (n == m) cout << "Yes" << endl;
  else cout << "No" << endl;
  
  return 0;
}