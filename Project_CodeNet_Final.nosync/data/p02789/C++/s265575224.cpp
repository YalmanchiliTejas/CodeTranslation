#include <bits/stdc++.h>
#define endl '\n';

typedef long long ll;

using namespace std;

const string YES = "Yes";
const string NO  = "No";

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(false);

    int n, m;

    cin >> n >> m;

    cout << (n == m ? YES : NO) << endl;

    return 0;
}

