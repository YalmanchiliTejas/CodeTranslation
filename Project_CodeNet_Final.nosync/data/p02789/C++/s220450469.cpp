#include <bits/stdc++.h>
#define  ll  long long
#define rep(k, n)  for(int i=k; i<n;  i++)
#define rep2(k,n)   for(int j=k; j<n;  j++)
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    cout << (n==m ? "Yes" : "No") << endl;
    return 0;
}