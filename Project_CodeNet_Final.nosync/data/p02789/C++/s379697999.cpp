#include "bits/stdc++.h"
using namespace std;
#define long int64_t

int main() {
	ios_base::sync_with_stdio( false );

	int N, M;
	cin >> N >> M;
	cout << (N==M ? "Yes" : "No") << endl;

	return 0;
}
