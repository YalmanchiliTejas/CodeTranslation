#include <bits/stdc++.h>
#define _GLIBCXX_DEBUG
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define REP(j, m) for (int j = 0; i < (int)(n); j++)
using ll = long long;
using namespace std;

int main() {
  int a,b;
  cin >>a>>b;
  if (a==b){
    cout <<"Yes"<<endl;
  }
  else {
    cout <<"No"<<endl;
  }}
