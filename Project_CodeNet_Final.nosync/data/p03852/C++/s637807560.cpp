#include <bits/stdc++.h>

#define pb push_back

using namespace std;

typedef long long ll;

const int maxn=1e3+10;

int main()
{
    ios_base::sync_with_stdio(false);
    char ch;
    cin>>ch;
    if(ch=='a' || ch=='i' || ch=='e' || ch=='o' || ch=='u')
        cout<<"vowel";
    else
        cout<<"consonant";
    return 0;
}
