#include <bits/stdc++.h>
using namespace std;

int main() {
  string S;
  
  int x,z;
  char y;
  
  cin >> y;
  if ( y == 'a' || y == 'e' || y == 'i' || y == 'o' || y == 'u' ) {
    cout <<"vowel" << endl;
  }
  else
    cout <<"consonant" << endl;
}
  

