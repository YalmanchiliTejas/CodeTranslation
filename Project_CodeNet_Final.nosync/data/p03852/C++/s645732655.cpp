#include<bits/stdc++.h>
using namespace std;

int main() {
    char ch;
    scanf("%c",&ch);
    if(ch=='a' || ch == 'e' || ch=='i' || ch=='o' || ch=='u') puts("vowel");
    else puts("consonant");
    return 0;
}