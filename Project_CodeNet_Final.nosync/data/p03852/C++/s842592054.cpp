#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main() {
	char c;
	cin >> c;

	cout << (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' ? "vowel" : "consonant") << endl;
}
