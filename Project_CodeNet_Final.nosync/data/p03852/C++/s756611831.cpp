#include <iostream>
#include <algorithm>
#include <string>
#include <cmath> //abs();
using namespace std;

int main(void)
{
    long long int a,b,c,d,i,j,k,n,x[10];
    char C;
    string s;

    cin >> C;

    if(C=='a' || C=='e' || C=='i' || C=='o' || C=='u') cout << "vowel";
    else cout << "consonant";
    return 0;
}
