#include<bits/stdc++.h>
using namespace std;
int main(){
  cin.tie(NULL);
  ios::sync_with_stdio(false);
  char s;
  cin>>s;
  if(s=='a' || s=='i' || s=='u' || s=='e' || s=='o') cout<<"vowel"<<'\n';
  else cout<<"consonant"<<'\n';
  return 0;
}
