#include <bits/stdc++.h>
using namespace std;

int main() {
    char E;
    cin >> E;
    
    if (E == 'a' || E == 'i' || E == 'u' || E == 'e' || E == 'o') {
        cout << "vowel" << endl;
    }
    else {
        cout << "consonant" << endl;
    }
        
}
