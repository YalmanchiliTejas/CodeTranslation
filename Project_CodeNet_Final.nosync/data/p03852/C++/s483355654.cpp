#include <bits/stdc++.h>
using namespace std;

int main(){
 char c;
 cin >> c;
  
 if (c != 'a' && c != 'i' && c != 'u' && c != 'e' && c != 'o'){
  cout << "consonant" << endl;
 }
 else{
  cout << "vowel" << endl;
 }
}