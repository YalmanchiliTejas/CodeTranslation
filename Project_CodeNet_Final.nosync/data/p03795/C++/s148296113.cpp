#include <bits/stdc++.h>
#include <set>
#define rep(i,n) for (int i=0; i < (n); ++i)
using namespace std;

double ceil(double);

int main() {
  int n; cin >> n;
  int x = 800 * n, y = n/15 * 200;
  cout << x-y << endl;
  return 0;

}