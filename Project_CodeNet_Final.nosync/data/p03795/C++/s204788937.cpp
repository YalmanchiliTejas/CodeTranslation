#define _USE_MATH_DEFINES
#include <bits/stdc++.h>
#define dump(x) cout << x << '\n'

typedef int64_t Int;
const double pi = M_PI;

using namespace std;

int main() {
    int n;
    cin >> n;

    cout << n * 800 - (n / 15) * 200 << endl;
}
