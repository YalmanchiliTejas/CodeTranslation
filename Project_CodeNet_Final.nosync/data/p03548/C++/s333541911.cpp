#include "bits/stdc++.h"
#define FOR(i,a,b) for(int i=(a);i<(b);i++)
#define REP(i,a) FOR(i,0,a)
typedef long long ll;

int INF = 1000000005;
using namespace std;

int main() {
    int x, y, z;
    cin >> x >> y >> z;
    cout <<  (x - z) / (y + z) << endl;

    

    return 0;
}