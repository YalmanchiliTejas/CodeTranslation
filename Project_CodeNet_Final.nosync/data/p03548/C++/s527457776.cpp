#define _USE_MATH_DEFINES
#include <bits/stdc++.h>
using namespace std;

#define int long long

int A, B, C;

signed main() {
    cin >> A >> B >> C;
    cout << (A - C) / (B + C) << endl;
    return 0;
}
