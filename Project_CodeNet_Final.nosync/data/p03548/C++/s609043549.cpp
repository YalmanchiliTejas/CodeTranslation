#include <iostream>
#include <algorithm>
#include <string>
#include <cmath>
#include <functional> 
#include <vector>

using namespace std;

int main(){

    int x,y,z;
    cin >> x;
    cin >> y;
    cin >> z; 

    cout << (x-z)/(y+z);

    return 0;
}