#include<iostream>
#include<algorithm>
#include<string>
#include<cstdlib>
#include<map>
#include<iomanip>
#include<sstream>
#include<vector>
#include<stack>
#include<math.h>
using namespace std;

int main() {
	map<long long int, long long int>mp;
	//cout << fixed << setprecision(10);
	long long int n, m, a[2000][2000] = {}, b, x, y, z, k, sum = 0, d = 1, w, h, typ = 0;
	long long dp[200005] = {}, memo[200005] = {};
	string str, str2;
	cin >> x >> y >> z;
	cout << (x - z) / (y + z);

}
