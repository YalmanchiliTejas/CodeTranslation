#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) x.begin(),x.end()

int main() {
   int x,y,z;
   cin >> x >> y >> z;
   cout << (x-z)/(y+z) << endl;
}