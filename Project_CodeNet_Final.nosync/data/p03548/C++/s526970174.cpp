#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <map>
#include <queue>
typedef long long ll;
using namespace std;
#define rep(i, n) for (int i = 0; i <= int(n); i++)
#define mod 1000000007
 
int main()
{
	int X,Y,Z;
	cin >> X >> Y >> Z;
	cout<< (X-Z) / (Y+Z) << endl;
}