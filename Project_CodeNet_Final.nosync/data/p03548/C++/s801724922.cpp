#include <bits/stdc++.h>
#include <math.h>
#define rep(i, n) for(int64_t i = 0; i < n; ++i)
using namespace std;

int main() {
  int x, y, z;
  cin >> x >> y >> z;
  cout << (x-z)/(y+z) << endl;
  return 0;
}