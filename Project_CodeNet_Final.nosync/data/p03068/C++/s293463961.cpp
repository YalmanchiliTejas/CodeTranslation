#include <bits/stdc++.h>
using namespace std;
int main()
{
int a, b;
string s;
char w;
cin >> a >> s >> b;
w = s[b-1];
for (int i = 0; i < s.size(); i++){
	if (s[i] == w)
	int mx = 0;
	else
	s[i] = '*';
}
cout << s;
}