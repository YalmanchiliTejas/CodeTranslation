#include <bits/stdc++.h>
using namespace std;

void solve() {
  int x; cin >> x;
  cout << (x >= 30 ? "Yes" : "No") << endl;
}

signed main() {
  cin.tie(nullptr);
  ios::sync_with_stdio(false);
  cout << fixed << setprecision(10);
  solve();
  return 0;
}
