#include <bits/stdc++.h>

using namespace std;

int main(void)
{
  ios::sync_with_stdio(false);
  cin.tie(0);
  int temperature;
  cin >> temperature;
  cout << ((temperature >= 30)? "Yes" : "No") << '\n';
  return 0;
}
