#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() {
    int c;
    cin >> c;
    if (c >= 30 && c <= 40)
    {
        cout << "Yes";
    }
    else if (c >= -40 && c < 30)
        cout << "No";
}