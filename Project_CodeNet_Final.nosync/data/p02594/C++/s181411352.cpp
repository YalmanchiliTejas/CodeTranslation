#include <iostream>
#include <array>
#include <functional>
#include <utility>
#include <regex>

int main()
{
    int64_t X;
    std::cin >> X;

    std::cout << (X >= 30 ? "Yes" : "No") << std::endl;

    return 0;
}

