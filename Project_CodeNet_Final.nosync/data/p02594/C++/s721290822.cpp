#include <bits/stdc++.h>
using namespace std;

void Main()
{
    int x;
    cin >> x;
    auto ans = x>=30 ? "Yes" : "No";
    cout << ans << endl;
}

int main(int argc, char **argv)
{
    ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    Main();
    return 0;
}
