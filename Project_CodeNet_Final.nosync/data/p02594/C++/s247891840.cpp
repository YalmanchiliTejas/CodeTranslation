#include <bits/stdc++.h>

using namespace std;



void solver() {
    int x; cin >> x;
    cout << (x >= 30 ? "Yes" : "No") << '\n';
}

int main() {

    ios_base :: sync_with_stdio(0); cin.tie(NULL);

    int t;
    //cin >> t;
    t = 1;

    while(t--)
        solver();

    return 0;
}