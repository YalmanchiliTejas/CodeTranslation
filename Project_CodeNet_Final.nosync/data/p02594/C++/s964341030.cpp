#include <bits/stdc++.h>

using namespace std;

#define rep(i, n) for(i = 0; i < n; ++i)
#define repr(i, n) for(i = 0; i > n; --i)
#define REP(i,k,n) for (i = k; i <= n; ++i) 
#define REPR(i,k,n) for (i = k; i >= n; --i)

int main()
{
	int n;
	cin >> n;
	if(n >= 30) cout << "Yes" << endl;
	else cout << "No" << endl;
}
