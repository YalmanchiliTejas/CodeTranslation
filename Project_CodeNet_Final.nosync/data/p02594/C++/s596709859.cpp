#include <bits/stdc++.h>

using namespace std;
vector <int> v;

int main() {
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(0);

    int x;
    cin >> x;
    if(x >= 30)
        cout << "Yes";
    else
        cout <<"No";
}