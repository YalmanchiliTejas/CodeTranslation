#include <bits/stdc++.h>
//#include <random>
#define int long long
#define double long double
#define endl "\n" 
const int INF = (int) 1e18 + 7;
const int maxn = (int) 1e4 + 6;
//const md = (int) 1e9 + 7;
//mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
using namespace std;
signed main() {
    int n; cin >> n;
    if(n >= 30) cout << "Yes" << endl;
    else cout << "No" << endl;
    return 0;
}

