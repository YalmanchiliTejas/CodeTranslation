#include <bits/stdc++.h>
using namespace std;

#define rep(i, a, b) for (int i = (a); i < (b); i++)

int main()
{
  int x;
  cin >> x;

  if (x >= 30)
    cout << "Yes" << endl;
  else
    cout << "No" << endl;

  return 0;
}