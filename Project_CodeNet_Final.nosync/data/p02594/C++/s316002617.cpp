// Alert!!! This program may have some NARGLE.
// Alert!!! Add a \ to the // at the beginning
//          of the main function if you're gonna use printf or scanf.
#include <bits/stdc++.h>

#define F first
#define pb push_back
#define S second
#define ll long long
#define ld long double
#define us unsigned
#define fileio freopen("input.txt", "r", stdin), freopen("output.txt", "w", stdout);
#define pii pair <int, int>

using namespace std;

const int Mod = 1e9 + 7;

int32_t main ()
{//
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
	int k;
	cin >> k;
	if (k >= 30)
		cout << "Yes";
	else
		cout << "No";
	return 0;
}