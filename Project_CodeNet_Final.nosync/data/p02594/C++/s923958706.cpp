#include <bits/stdc++.h>

using namespace std;

#define intt long long
#define FAST ios_base::sync_with_stdio(0); cin.tie(0);

int main() {

	FAST;

	int x;
	cin >> x;
	cout << ((x >= 30) ? "Yes\n" : "No\n");
	return 0;
}