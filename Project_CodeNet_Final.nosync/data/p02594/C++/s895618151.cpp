#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

#define FOR(i,a,b) for(ll i=a; i<b; i++)
#define IN(a) cin>>a
#define OUT(a) cout<<a<<endl

int main(){
  int x; IN(x);
  if(x>=30){
    OUT("Yes");
  }else{
    OUT("No");
  }
}