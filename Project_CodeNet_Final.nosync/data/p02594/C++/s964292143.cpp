#include <bits/stdc++.h>
#define rep(i,n) for (long long i = 0;i < (n);i++)
using namespace std;
typedef long long ll;

int main() {
   int n;
   cin >> n;
   if(n >= 30) {
      cout << "Yes" << endl;
      return 0;
   }
   cout << "No" << endl;
   return 0;
}