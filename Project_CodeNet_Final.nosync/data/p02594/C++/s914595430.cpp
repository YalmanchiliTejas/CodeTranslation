#include<bits/stdc++.h>
#define ll long long 
using namespace std;

int main()
{
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int t=1;
	long n;
	ll temp;

	while(t--)
	{
		cin>>temp;
		if(temp>=30) cout<<"Yes";
		else cout<<"No";
	}

	return 0;
}