#include <bits/stdc++.h>

using namespace std;

int main () {
  ios::sync_with_stdio(0);
  cin.tie(0);

  int X;
  cin >> X;
 	
  	if (X < 30) {
    	cout << "No" << endl;
    }

  else { 
    cout << "Yes" << endl;
  }
}
