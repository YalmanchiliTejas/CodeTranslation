#include<bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int TC = 1;
    for (int tc = 0; tc<TC; ++tc) {
        int N; 
        cin >> N;
        if (N>=30) cout << "Yes";
        else cout << "No";
    }
}