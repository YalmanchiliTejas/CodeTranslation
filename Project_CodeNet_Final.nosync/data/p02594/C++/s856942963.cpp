#include <bits/stdc++.h>

using namespace std;
#define FAST ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0), cout.setf(ios::fixed), cout.precision(20)
#define file freopen("in.txt", "r", stdin), freopen("out.txt", "w+", stdout)
#define endl '\n'

int main() {

    //file;
    FAST;

    int a;
    cin >> a;

    if (a >= 30) cout << "Yes\n";
    else cout << "No\n";

    return 0;
}
