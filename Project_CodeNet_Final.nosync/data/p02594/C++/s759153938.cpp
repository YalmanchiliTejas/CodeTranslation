#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
#define em emplace_back

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int x;
	cin >> x;
	if (x >= 30)
		cout << "Yes";
	else
		cout << "No";
	return 0;
}
