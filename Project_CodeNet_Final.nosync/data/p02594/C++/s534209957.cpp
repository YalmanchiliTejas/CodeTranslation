#include<iostream>
#define F(i,n) for(int i=0;i<n;i++)
#define lli long long int
// #define vi vector<int>
// #define vpb(x) v.push_back(x)
// #define spb(x) s.push_back(x)
using namespace std;

int main()
{
    int n; cin>>n;
    if(n>=30)
    cout<<"Yes";
    else
    {
        cout<<"No";
    }
    
    return 0;
}