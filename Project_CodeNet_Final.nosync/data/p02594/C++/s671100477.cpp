#include <iostream>
#include <sstream>
#include <cstdio>
#include <cmath>
#include <cstring>
#include <cctype>
#include <string>
#include <vector>
#include <list>
#include <set>
#include <map>
#include <queue>
#include <stack>
#include <algorithm>
#include <functional>
#include <bits/stdc++.h>
#include <stdio.h>
#include <iterator>

#include <string.h>
using namespace std;

#define DEBUG(x) cout << '>' << #x << ':' << x << endl;
#define REP(i,n) for(int i=0;i<(n);i++)
#define FOR(i,a,b) for(int i=(a);i<(b);i++)
#define FORD(i,a,b) for(int i=(a);i>(b);i--)
#define scn(a)  scanf("%d",&a)
#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define vecout(vec) for(int i=0;i<vec.size();i++){cout<<vec[i]<<" ";} 
#define cont continue
typedef long long ll;


const ll mod =1e9 +7;
int main(){
    fastio
    int n;
    cin>>n;
    
    if(n>=30) cout<<"Yes"<<'\n';
    else cout<<"No"<<'\n';
    
    
    
    return 0;
}
