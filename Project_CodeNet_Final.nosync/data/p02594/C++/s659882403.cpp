#include <bits/stdc++.h>
#define rep(i,a,n) for (int i = (a); i < (n); i++)
using namespace std;
typedef long long ll;
typedef pair<int,int> P;

int main(){
  int c;
  cin >> c;
  if(c>=30) cout << "Yes" << endl;
  else cout << "No" << endl;
  return 0;
}