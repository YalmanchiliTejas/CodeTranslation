#include <bits/stdc++.h>
#define all(X) X.begin(), X.end()
//for(int i = 0; i < n; i++)
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

    int n; cin >> n;
    if(n >= 30)
        cout << "Yes";
    else
        cout << "No";
}
