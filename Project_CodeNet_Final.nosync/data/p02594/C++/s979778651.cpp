//NEVER LOSE HOPE 
#define fastio ios_base::sync_with_stdio(0);cin.tie(NULL);cout.tie(NULL)
#include <bits/stdc++.h>
#define int long long int
#define vi vector<int>
#define vc vector<char>
#define pi pair<int,int>
#define si set<int>
#define mi map<int,int>
#define um unordered_map<int,int>
#define ff first
#define ss second
#define pb push_back
#define all(v) v.begin(),v.end()
#define gi greater<int>()
#define cont continue
#define mp make_pair
#define endl "\n"
#define lb lower_bound //a,a+n,4 -a==index of 4  // returns pointer to the "first position of num" if number is present in the container. Returns pointer to the "position of next higher number greater than num" if num is not present.   
#define ub upper_bound // returns pointer to the "first position of the next higher number than last occurence of num" if num is present in the container. Returns pointer to the position of next higher number than num if container does not contain occurence of num.
#define REP(i,n) for(int i=0;i<n;i++)
#define REPM(i,n) for(int i=0;i<n-1;i++)
#define REPE(i,n) for(int i=1;i<=n;i++)
#define REPA(i,a,n) for(int i=a;i<n;i++)
 
#define ip(a) int n;cin>>n;int a[n];REP(i,n)cin>>a[i];
#define op(v) for(auto x:v) cout<<x<<" ";cout<<endl;
#define opp(a)  REP(i,n)cout<<a[i]<<" "; cout<<endl;
   
#define vs(s)  cout<<v.size()<<" "; 
//next_permutation(s.begin(),s.end())
//*max_element(a,a+n)
#define mod 1000000007
using namespace std;

// bool isPrime(int n)
// {if(n==1)return false;
// for(int i=2;i*i<=n;i++)
// {if(n%i==0)return false;}
// return true;
// } 

// int gcd(int a,int b)
// {
//     if(b==0)return a;
//     return gcd(b,a%b);
// }
// int lcm(int a,int b)
// {
//     return a/gcd(a,b)*b;
 
// }  
 void CSK(){
          int n;cin>>n;
          if(n>=30)
          {
            cout<<"Yes"<<endl;
          }
          else
          {
            cout<<"No"<<endl;
          }
















}
  
  
  
 

 
 
 
int32_t main()
{
    #ifndef ONLINE_JUDGE
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    #endif 
    fastio;
    cout<<fixed;
    cout.precision(10);
    int __TESTCASES=1;
    //cin>>__TESTCASES;
    while(__TESTCASES--){
    CSK();  
}
 
}