#include<bits/stdc++.h>
using namespace std ;
typedef long long int ll;
#define in(n) ll n;cin>>n;
#define fo(m,n) for(int i=m;i< n;i++)
#define rfo(m,n) for(ll i =n;i>=m;i--)
#define out(n) cout<<n<<endl;
#define max(a,b) ((a>b)?a:b)

int main ()
{
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL);
    int t;cin>>t;
    if(t<30){cout<<"No"<<endl;}
    else
    {
        cout<<"Yes"<<endl;
    }
    return 0;
}