#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep2(i, s, n) for (int i = (s); i < (int)(n); i++)

int main() {
  int x;
  cin >> x;

  // ここにプログラムを追記
  if (x >= 30) {
    cout << "Yes" << endl;
  }
  else {
    cout << "No" << endl;
  }
}
