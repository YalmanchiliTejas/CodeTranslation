#include <bits/stdc++.h>
#define ll long long int

#define mx 100007
using namespace std;

int main()
{
    ll t,n,item,temp,a,b,c,x,y,z,sum,flag,k;
    string s,s1,s2;
    vector<ll>v;
    vector<string>vs;
    map<ll,ll>mp;
    map<ll,ll>check;

    ll arr[mx];
   vector<pair<ll,ll> >vp;
   cin>>n;
   if(n<30)
    cout<<"No"<<endl;
   else
    cout<<"Yes"<<endl;

    return 0;
}
