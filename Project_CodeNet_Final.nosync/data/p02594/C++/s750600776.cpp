// A.

#include <iostream>
#include <sstream>
#include <algorithm>
#include <vector>

using namespace std;

int main(int argc, char* argv[]) {
    int x;
    cin >> x;
    string ans = x >= 30 ? "Yes" : "No";
	cout << ans << endl;
	return 0;
}
