#include <bits/stdc++.h>

#define rep(x, n) for(int i = x; i < (n); ++i)

using namespace std;
using ll = long long;

int main(void)
{
  int n;
  cin >> n;
  cout << (n >= 30 ? "Yes" : "No") << endl;
}