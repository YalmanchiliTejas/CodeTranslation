#include <bits/stdc++.h>
using namespace std;
using ll = long long ;
#define pb push_back
#define mp make_pair
#define pf push_front
#define FOR(i,a,b)           for(int i = a;   i <  (int)(b); ++i)
#define for_rev(i,a,b)       for(int i = b-1; i >= (int)(a); --i)
#define for_it(it, v) for (auto it = v.begin(); it != v.end(); it++)
#define all(v) v.begin(), v.end()
#define sortv sort(all(v))
#define print(v)     \
    for (auto i : v) \
        cout << i << " ";
#define FAST                     \
    ios::sync_with_stdio(false); \
    cin.tie(NULL);               \
    cout.tie(NULL);
typedef pair<int,int> pi;
typedef vector<int> vi;
typedef unordered_set<int> ust;
typedef set<int> st;
typedef unordered_map<int, int> ump;
typedef map<int, int> mi;
#define F first
#define S second
/*****************************************************************************/

int main()
{
	int n ; cin >> n ;
	if(n>=30) cout << "Yes" << endl;
	else cout << "No" << endl;
return 0 ;
}	