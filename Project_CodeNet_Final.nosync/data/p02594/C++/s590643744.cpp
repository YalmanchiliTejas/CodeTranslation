#include <bits/stdc++.h>
#define rep(i, a, b) for (int i = (int)(a); i < (int)(b); ++i)
using namespace std;

int main()
{
    int t;
    cin >> t;

    if (t >= 30)
    {
        std::cout << "Yes" << endl;
    }
    else
    {
        std::cout << "No" << endl;
    }

    return 0;
}
