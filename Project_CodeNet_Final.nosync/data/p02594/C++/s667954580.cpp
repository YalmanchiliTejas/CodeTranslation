#define _USE_MATH_DEFINES
#include <bits/stdc++.h>
using namespace std;
signed main() { 
  ios::sync_with_stdio(false); cin.tie(0);
  //a
  int x;
  cin >> x;
  cout << (x >= 30 ? "Yes\n" : "No\n");
  return 0;
}