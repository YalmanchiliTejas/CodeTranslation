#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vl = vector<ll>;
using vvl = vector<vl>;
using vb = vector<bool>;


int main() {
    cin.sync_with_stdio(0);
    ll t; cin >> t;
    if(t >= 30) cout << "Yes" << endl;
    else cout << "No" << endl;
}