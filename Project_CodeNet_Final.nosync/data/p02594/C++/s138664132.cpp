// { Driver Code Starts
#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);
  int x;
  cin >> x;
//   cout << ((x>=30) ? (cout << "Yes"; ) : (cout << "No";));	
if (x >= 30)
cout << "Yes";
else 
cout << "No";
	return 0;
}
