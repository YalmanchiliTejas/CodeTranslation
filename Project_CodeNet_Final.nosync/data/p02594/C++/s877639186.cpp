#include <bits/stdc++.h>
#define rep(i,n) for(long long i=0; i<n; i++)
#define Rep(i,n) for(long long i=1; i<n; i++)
#define ll long long
using namespace std;

ll max2val(ll a, ll b) {
    if (a > b) {
        return a;
    }
    else {
        return b;
    }
}

int main() {
 ll X;
 cin>>X;
 if(30<=X){
     cout<<"Yes"<<endl;
 }else{
     cout<<"No"<<endl;
 }
 }

