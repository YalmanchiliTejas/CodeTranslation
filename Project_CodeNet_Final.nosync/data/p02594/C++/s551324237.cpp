#include <bits/stdc++.h>

#define mod 1000000007
#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define rep(var,start,end) for(int var=start; var<end; var++)
#define repr(var,start,end) for(int var=start; var>=end; var--)
#define repi(var,start,end) for(int var=start; var<=end; var++)
#define debg(var) cerr<<#var<<" : "<<var<<" "
#define pb push_back
#define mp make_pair

using namespace std;

typedef unsigned long long ull;
typedef long long ll;

void solve() {
    int X;
    cin>>X;
    if(X>=30)
        cout<<"Yes";
    else
        cout<<"No";
}

int main() {
    fastio
   
    int t = 1;
    //cin>>t;
    while(t--) {
        solve();
    }
    return 0;
}