#include <cstdio>
#include <iostream>

using namespace std;

typedef long long ll;
typedef double db;

#define dbg(x) cout<<#x<<" is "<<x<<endl

int main() {
    int x;
    cin >> x;
    printf("%s\n", x >= 30 ? "Yes" : "No");
    return 0;
}