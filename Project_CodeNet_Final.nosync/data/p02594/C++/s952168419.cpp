#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (int)(n); i++)
using namespace std;

signed main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int x;
  cin >> x;

  if(x >= 30) {
    cout << "Yes" << endl;
  } else {
    cout << "No" << endl;
  }
}