#include <bits/stdc++.h>
using namespace std;
#define fori(a,b) for(int i=a;i<b;i++)

int main() {
     ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin>>n;
   cout<<( n>=30 ? "Yes" : "No");
     return 0;
}
