#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(void){
    long long int i, j, n, x, ans=0, po=0;
    scanf("%lld", &x);
    if(x>=30) printf("Yes");
    else printf("No");
    
}