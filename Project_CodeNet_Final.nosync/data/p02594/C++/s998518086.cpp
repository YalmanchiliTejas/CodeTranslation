#include <iostream>
using namespace std;

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    int x;
    cin >> x;

    if(x >= 30) puts("Yes");
    else puts("No");

    return 0;
}