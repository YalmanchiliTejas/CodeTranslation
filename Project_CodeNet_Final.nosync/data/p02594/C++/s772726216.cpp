#include <bits/stdc++.h>
using namespace std;
using vi = vector<int>;
using vc = vector<char>;
using vs = vector<string>;
using vvi = vector<vector<int>>;
using vvc = vector<vector<char>>;

int main()
{
    int X;
    cin >> X;
    if(X>=30)
        cout << "Yes" << endl;
        else
        {
            cout << "No" << endl;
        }
    return 0;
}