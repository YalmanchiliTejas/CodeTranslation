#include "bits/stdc++.h"

using namespace std;

void Main() {
    int X;
    cin >> X;
    cout << (X >= 30 ? "Yes" : "No") << endl;
}

int main() {
    std::cout << std::fixed << std::setprecision(15);
    Main();
    return 0;
}
