#include<bits/stdc++.h>
using namespace std;
#define repi(i,x,n) for(int i=x;i<n;i++)
#define rep(i,n) repi(i,0,n)

using ll = long long;
using P = pair<int,int>;

int main(){
    int x;
    cin >> x;
    if (x >= 30){
        cout << "Yes" << endl;
    } else{
        cout << "No" << endl;
    }

    return 0;
}