#include <bits/stdc++.h>

using namespace std;
using ll = long long;
int n;
int main(){
    ios_base::sync_with_stdio(0);cin.tie(nullptr);
    cin>>n;
    if(n >= 30 ) cout<<"Yes";
    else cout<<"No";
}
