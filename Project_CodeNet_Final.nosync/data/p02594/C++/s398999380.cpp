#include <bits/stdc++.h>
#define rep(i,n) for(int i=0; i< (n) ; ++i)
using namespace std;
using l = long long;
using P = pair<int,int>;
int main(){
  int n;
  scanf("%d",&n);
  if(30<=n) printf("Yes");
  else printf("No");
    return 0;
}