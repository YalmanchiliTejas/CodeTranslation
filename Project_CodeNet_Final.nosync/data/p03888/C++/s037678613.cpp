#include <bits/stdc++.h>
using namespace std;
using Int = long long;
int main()
{
    double R1, R2; cin >> R1 >> R2;
    cout << fixed << setprecision(9) << (R1 * R2) / (R1 + R2) << endl;
    return 0;
}
