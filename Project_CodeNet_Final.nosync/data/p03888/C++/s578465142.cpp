#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef double dd;

int main(){
  double a, b;
  cin >> a >> b;
  cout << setprecision(20) << a * b / (a + b) << endl;
  return 0;
}
