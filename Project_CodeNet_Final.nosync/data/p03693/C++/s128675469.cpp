#include <iostream>
#include <string>
using namespace std;

int main(){
    string s;
    cin >> s[0] >> s[1] >> s[2];
    int ss = atoi(s.c_str());
    if(ss % 4 == 0) cout <<"YES"<<endl;
    else cout <<"NO" << endl;
    
}