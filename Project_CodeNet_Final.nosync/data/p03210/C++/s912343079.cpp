#include <bits/stdc++.h>
#define sz(arr) (int)arr.size()
#define all(arr) arr.begin(), arr.end()

using namespace std;

int main() {
  int x;
  cin >> x;

  puts(x == 3 || x == 5 || x == 7 ? "YES" : "NO");

  return 0;
}