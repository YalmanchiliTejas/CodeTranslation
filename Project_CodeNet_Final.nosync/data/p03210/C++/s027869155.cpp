#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio( false );

	char X;
	cin >> X;
	cout << (X == '7' || X == '5' || X == '3' ? "YES" : "NO") << endl;

	return 0;
}
