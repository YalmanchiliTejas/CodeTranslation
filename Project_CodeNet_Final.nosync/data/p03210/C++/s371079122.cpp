#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for(int i=0; i<n; i++)

int main() {
    int x; cin >> x;
    cout << (x == 7 || x == 5 || x == 3 ? "YES" : "NO") << endl;
    return 0;
}
