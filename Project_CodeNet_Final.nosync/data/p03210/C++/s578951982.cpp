#include <bits/stdc++.h>
using namespace std;

#define rp(i, k, n) for (ll i = k; i < n; i++)
#define ll  long long

ll mod = 1e9 + 7;

int main() {
    int x;
    cin >> x;
    if (x == 3 || x == 5 || x == 7) {
        printf("YES");
        return 0;
    } else {
        printf("NO");
        return 0;
    }
}
