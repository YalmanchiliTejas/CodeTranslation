#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
typedef long long ll;


int main() {
    int X;
    cin >> X;
    string ans = "NO";
    if (X == 3 || X == 5 || X == 7) ans = "YES";
    cout << ans << endl; 
}
