#include <bits/stdc++.h>

#define rep(x, y) for (int i = (x); i < (y); i++)

typedef long long ll;

using namespace std;

int main() {
    int x;
    cin >> x;

    if (x == 3 || x == 5 || x == 7)
        cout << "YES";
    else
        cout << "NO";
    cout << endl;

    return 0;
}