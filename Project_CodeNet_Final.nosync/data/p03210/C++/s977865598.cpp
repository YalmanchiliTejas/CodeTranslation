#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
typedef long long ll;
int main() {
    // ifstream in("data1.txt");
    // cin.rdbuf(in.rdbuf());
    int X;
    cin >> X;
    if(X == 7 || X == 5 || X == 3)
        cout << "YES" << endl;
    else {
        cout << "NO" << endl;
    }
}
