#include <bits/stdc++.h>
using namespace std;
using i64 = int64_t;
using vi = vector<i64>;
using vvi = vector<vi>;

int main() {
    int x;
    cin >> x;
    cout << ((x == 3 || x == 5 || x == 7) ? "YES" : "NO") << endl;
}