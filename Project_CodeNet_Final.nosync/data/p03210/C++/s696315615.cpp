#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    int x;
    cin >> x;
    cout << ((x == 7) ? "YES" : ((x == 5) ? "YES" : ((x == 3) ? "YES" : "NO")));
    return 0;
}