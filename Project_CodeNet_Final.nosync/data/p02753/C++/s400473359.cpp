#include <bits/stdc++.h>
using namespace std;
#define rep(i,n); for(long long i = 0;i < (n);i++)
using ll = long long;
using vl = long long;
using P = pair<long long,long long>;

int main(){
	string s; cin >> s;
	if(s == "AAA" || s == "BBB")cout << "No" << endl;
	else cout << "Yes" << endl;

}