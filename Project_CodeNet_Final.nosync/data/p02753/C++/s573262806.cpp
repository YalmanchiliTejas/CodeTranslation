#include <bits/stdc++.h>

#define rep(i,c) for(int i = 0; i < (int)c; ++i)
using llint = long long int ;
using namespace std;


int main(){
    string S;
    cin>>S;
    if (S[0]==S[1]&&S[0]==S[2]) cout<<"No"<<endl;
    else cout<<"Yes"<<endl;
}