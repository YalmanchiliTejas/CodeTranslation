#include <bits/stdc++.h>

using namespace std;

#define fori(x) for (int i = 0; i < x; ++i)
#define forj(x) for (int j = 0; j < x; ++j)

int main() {
	string s;
	cin >> s;
	if (s == "AAA" || s == "BBB") {
		cout << "No";
	} else {
		cout << "Yes";
	}
	return 0;
}