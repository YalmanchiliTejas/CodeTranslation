#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int (i) = 0; (i) <(n); (i)++) 
#define so(v) sort(v.begin(),v.end())
using ll = long long;
using vi=vector<int>;
using vii=vector<vi>;
int main() {
   int a,b,c,n,m,x,y;
   string s;cin>>s;
   if(s=="AAA"||s=="BBB")cout<<"No";
   else cout<<"Yes";
}