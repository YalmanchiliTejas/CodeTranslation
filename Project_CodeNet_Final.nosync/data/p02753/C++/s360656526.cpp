#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(){
  cin.tie(0);
  ios::sync_with_stdio(false);
  string s; cin>>s;
  if(s=="AAA"){
    cout<<"No"<<endl;
  }else if(s=="BBB"){
    cout<<"No"<<endl;
  }else cout<<"Yes"<<endl; 
}
