#include<bits/stdc++.h>

#define reg register

using namespace std;

string s;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0),cout.tie(0); 
	cin>>s;
	if(s=="AAA" || s=="BBB") puts("No");
	else puts("Yes");
	return 0;
}