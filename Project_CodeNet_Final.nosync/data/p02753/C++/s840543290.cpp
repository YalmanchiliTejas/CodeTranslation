#include <iostream>
#include<vector>
#include<map>
#include<set>
#include<stack>
#include<queue>
#include<algorithm>
#include<cmath>
#define ll long long
#define pb push_back
#define pii pair<int,int>
#define vi vector<ll>

#define pll pair<long long,long long>
#define pis pair<int,string>
#define mp make_pair
#define F first
#define S second
#define T int tt; cin>>tt; while(tt--)
#define fastio ios_base::sync_with_stdio(false);

#define endl '\n'
#define all(a) (a).begin(), (a).end()
#define display(x) for(auto i : x) cout << i << " "; cout << '\n';
#define input(x) for(int i = 0; i < n; i++) cin >> x[i];
#define make(x,y) memset(a, y, sizeof(x))
#define FO1(i,n) for (int i = 1; i <= n; i++)
#define FO(i,n) for (int i = 0; i < n; i++)
#define mod 1000000007
#define Y cout<<"YES"<<endl;
#define N cout<<"NO"<<endl;

using namespace std;

int main()
{
    string s; cin>>s;
    if(s=="BBB" || s=="AAA")  cout<<"No";
    else cout<<"Yes";
   	return 0;
}












