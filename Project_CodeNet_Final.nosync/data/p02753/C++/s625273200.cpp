#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);++i)
using ll = long long;
using pii = pair<int, int>;
using vi = vector<int>;
using vii = vector<vector<int>>;

int N;
string s;

int main(){
    cin >> s;
    if(s == "AAA") cout << "No";
  else if(s == "BBB") cout << "No";  
  else cout << "Yes";
	return 0;
}