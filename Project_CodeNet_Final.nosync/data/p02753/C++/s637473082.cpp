#include <bits/stdc++.h>
#define ll long long
#define ld long double
#define ctoi(x) x-'0'
#define pshb(a) cin >> x;a.push_back(x)
#define fr(i,n) for(ll i=0;i<n;i++)
#define fast ios_base::sync_with_stdio(false);
#define Test ll q ; cin>>q ; while (q--)
#define ph push_back
#define f first
#define s second
using namespace std ;
int main ()
{
    string s ;
    cin>>s ;
    if (s.find('A')!=string::npos&&s.find('B')!=string::npos)
        cout<<"Yes" ;
    else
        cout<<"No" ;
}
