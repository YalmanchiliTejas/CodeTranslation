#include <bits/stdc++.h>
#define rep(i,n) for(int i=0;i<(n);i++)
using namespace std;
using p = pair<int, int>;

int main(){
  string S;
  cin >> S;
  if(S=="AAA" or S=="BBB") cout << "No";
  else cout << "Yes";
}