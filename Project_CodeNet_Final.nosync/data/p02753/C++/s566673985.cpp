/*
START BY THE NAME OF ALMIGHTY ALLAH
THIS WONT BE ACCEPTED
STOP_GIVING_UP
*/
#include<bits/stdc++.h>
#define l(i,a,n)for(int i=a;i<n;i++)
#define PB push_back
#define IN insert
#define MP make_pair
#define all(v) v.begin(),v.end()
//#define y cout<<"Yes"
#define LW(v) sort(v.begin(),v.end());
#define HI(v) sort(v.begin(),v.end(),greater<long long>());
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    double t,r=1,r1=0,r2=0,k=0,a,b,c,m,d=0,n,e,f,x,g,p=0,q=0,y=0,z=0;
    vector<long long>v;
    vector<long long>u;
    set<long long>s;
    std::vector<int>::iterator it;
    string  s1,s2,s3,s4,s5;
    cin>>s1;
    a=count(all(s1),'A');
    b=count(all(s1),'B');
    if(a>=1&&b>=1)
    {
        cout<<"Yes";
    }
  else{cout<<"No";
//cout<<setprecision(12)<<fixed<<x<<endl;
}
}



