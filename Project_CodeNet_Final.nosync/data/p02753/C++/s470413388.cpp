#include <queue>
#include <stack>
#include <map>
#include <algorithm>
#include <math.h>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <complex>
typedef long long int LL;
using namespace std;

// 插入此處

int main() {
    char s[5];
    scanf("%s", s);
    if (s[0] == s[1] && s[1] == s[2]) {
        printf("No\n");
    } else {
        printf("Yes\n");
    }
}
