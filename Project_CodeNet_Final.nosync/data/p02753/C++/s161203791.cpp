#include <bits/stdc++.h>
using namespace std;
using pii=pair<int,int>;
using ll=long long;

#define fi first
#define se second
#define pb push_back
#define rep(i,n) for(int i=0; i<(int)(n); i++)

int main() {
	string s;
	cin >> s;
	if(s != "AAA" && s != "BBB") cout << "Yes" << endl;
	else cout << "No" << endl;

  return 0;
}
