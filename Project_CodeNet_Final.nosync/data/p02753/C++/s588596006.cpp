#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define endl '\n'

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string s;
    cin >> s;
    if (s != "AAA" && s != "BBB") cout << "Yes" << endl;
    else cout << "No" << endl;
    return 0;
}