#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int (i)=0; (i)<(n); (i)++)
#define all(v) (v).begin(), (v).end()

int main() {
    string s;
    cin >> s;

    if(s == "AAA" || s == "BBB"){
        cout << "No";
    }
    else{
        cout << "Yes";
    }
}