#include<bits/stdc++.h>
#define rep(i, n) for (int i=0;i<(n);i++)
#define P pair<int, int>
#define ll long long
using namespace std;

int main() {
  string S; cin>>S;
  if ((S[0]==S[1])&&(S[1]==S[2])) cout<<"No"<<endl;
  else cout<<"Yes"<<endl;
}
