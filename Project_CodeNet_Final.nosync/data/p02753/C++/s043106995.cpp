#include <bits/stdc++.h>
using namespace std;

int main() {
	int a, ans;
	string s;
	cin >> s;
	if (s.at(1) == s.at(2) && s.at(0) == s.at(1))cout << "No";
	else cout<<"Yes";

	//cout << ans << "\n";
	return 0;
}
