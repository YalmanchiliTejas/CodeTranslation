#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    string s;
    cin >> s;
    cout << ((s == "AAA" || s == "BBB") ? "No" : "Yes") << endl;
    return 0;
}